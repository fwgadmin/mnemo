/**
 * Bundled default user guardrails (tone/style). When the user leaves the field empty,
 * main uses this text. The summarization mandate is applied separately in code.
 */
export const DEFAULT_USER_GUARDRAILS = `Write a neutral, factual summary of the supplied text only. Do not add facts, opinions, or advice not present in the text. Preserve important names, numbers, and dates when they matter for understanding.`;
