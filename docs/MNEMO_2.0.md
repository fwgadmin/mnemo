# Mnemo 2.0 planning (pointer)

**Source of truth:** the vault under **`Projects/Mnemo-2.0`** (and subpaths), maintained via the desktop app or **MCP** (`create_note`, `update_note`, `read_note`, etc.). This file is only a **pointer** for contributors who browse the git repo without MCP.

**Canonical rules:** vault note **Directives** (first tag `Projects/Mnemo-2.0/Directives`) — structure, thematic grouping, persistence, MCP workflow. **Index** links there first.

| Note (title) | Category path (first tag) |
|--------------|---------------------------|
| Index | `Projects/Mnemo-2.0` |
| Directives | `Projects/Mnemo-2.0/Directives` |
| Roadmap | `Projects/Mnemo-2.0/Roadmap` |
| Backlog | `Projects/Mnemo-2.0/Backlog` |
| Log | `Projects/Mnemo-2.0/Log` |

Original brainstorm: vault note **“mnemo features”** under `Projects`.

**Cursor:** project rule `.cursor/rules/mnemo-vault-planning.mdc` (always on) encodes the same expectations for agents.

**Convention:** After substantive work, agents prepend a dated entry to **Log** and update **Backlog** checkboxes.
